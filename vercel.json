{
    "version": 2,
    "builds": [
      {
        "src": "api/index.ts",
        "use": "@vercel/node"
      },
      {
        "src": "wokilite-frontend/package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "wokilite-frontend/dist"
        }
      }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/index.ts" },
      { "src": "/(.*)", "dest": "/wokilite-frontend/dist/$1" }
    ]
  }
  